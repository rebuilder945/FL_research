def count_foreign(ids):
    m=[x for x in idx]
    lst=[]
    for x in range ids:
        if x[0]=L
        lst=lst.append(x)
    num=len(lst)
    return num

origin=input().split()
print(count_foreign(origin))

