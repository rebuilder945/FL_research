def count_foreign(ids):
    X=[x for x in origin]
    a=0
    for x in X:
                if x[0]=“L”:
                          a+=1
                   


origin=input().split()
print(count_foreign(origin))

