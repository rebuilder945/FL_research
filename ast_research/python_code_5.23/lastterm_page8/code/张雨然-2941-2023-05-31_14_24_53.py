def count_foreign(ids):
    m=[x for x in idx]
    lst=[]
    for x in ids:
        if x[0]in['L']
        lst=lst.append(x)
    num=len(lst)
    return num

origin=input().split()
print(count_foreign(origin))

