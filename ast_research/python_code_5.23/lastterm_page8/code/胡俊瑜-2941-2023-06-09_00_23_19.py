def count_foreign(ids):
    lst=[eval(x) for x in ids]
    for i in lst:
        a=0
        If "L" in i：
            a+=1
    return a
            

origin=input().split()
print(count_foreign(origin))

