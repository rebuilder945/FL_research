def count_foreign(ids):
    X=[x for x in origin]
    a=0
    for x in X:
                if len(x)==10:
                          a+=1
    return a
                   


origin=input().split()
print(count_foreign(origin))

