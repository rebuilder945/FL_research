def maxsum(x):
    x.sort()
    long = len(x)
    eva = 0
    for x1 in range(1,long+1,2)
        eva = eva + x[x1-1]
    return eva





nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

