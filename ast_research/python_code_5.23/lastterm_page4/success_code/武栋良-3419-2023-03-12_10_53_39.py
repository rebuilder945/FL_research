def calDegrees(nums):
    nums = list(nums)
    for x in nums:
        numss = nums.reverse(nums.sort(key = nums.count(x)))
    e = numss.count[0]
    return e



nums = eval(input())
d=calDegrees(nums) #调用自定义函数
print(d)

