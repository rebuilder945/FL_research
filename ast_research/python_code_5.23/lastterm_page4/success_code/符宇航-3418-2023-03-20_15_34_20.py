
def maxsum(nums):
    letter = nums
    n = 2
    www = [letter[i:i+n] for i in fuck]
    for x in range(len(www)/2):
        b = min(x)
        print(b)
        shift = []
        shift.append(b)
    qq = max(shift)
    return qq





nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

