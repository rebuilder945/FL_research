def maxsum(nums):
   for a in range(n):
        x=min(nums)
        sum_min+=x
   for b in range(n):
        y=max(nums)
        sum_max+=y




nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

