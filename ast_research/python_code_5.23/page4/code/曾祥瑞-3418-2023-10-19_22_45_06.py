def maxsum(nums):
    for a in nums:
        x=random.randint(0,len(nums)-1）
        for b in nums:
            y=random.randint(0,len(nums)-1）
            m=min(x,y)
            w=0
            w+=m
        return v




nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

