from collections import Counter
def calDegrees(nums):
      count = Counter(nums）
      max-freq=max(count.values())
      return max-freq
             


nums = eval(input())
d=calDegrees(nums) #调用自定义函数
print(d)

