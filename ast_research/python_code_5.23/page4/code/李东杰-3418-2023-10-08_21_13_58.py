def maxsum(nums)
    for x in range(0,n):
    a=random.randint(0,2n) 
    nums1=nums.pop(a)
    b=min(nums)
    c=min(nums1)
    return d=max(b+c)

    





nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

