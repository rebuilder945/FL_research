import random

def maxsum(a):
    random.shuffle(a)
    max=0
    for i in range(0,len(a),2)
        max=max+min(a(i),a(i+1))
    return (max)




nums = eval(input())
v = maxsum(nums)#调用自定义函数
print(v)

