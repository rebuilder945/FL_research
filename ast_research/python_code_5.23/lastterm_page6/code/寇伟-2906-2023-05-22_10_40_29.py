def main():
    total_count = int(input())
    calculate_days(total_count)
from math import *
def calculate_days(w):
    days = 0  # 计数器，记录卖出的天数

    while total_count  > 0:
        days += 1
        if n == 1:  # 若只剩下一个西瓜，则直接卖出并结束循环
            break
        n = (n // 2) + 2

    print(days）
        
main()


